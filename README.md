1. python -m venv venv
2. venv\Scripts\activate
3. (If error occurs on step 2, run the ff and run step 2 again) Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
4. pip install -r requirements.txt
5. ./proto_compile.bat
6. use run.bat
7. open localhost:5000
